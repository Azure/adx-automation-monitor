language: python
python:
- '3.6'
services:
- docker
install:
- pip install -r requirements.txt
script:
- pylint app/app
deploy:
  on:
    all_branches: true
    condition: $TRAVIS_BRANCH =~ ^dev|master$
  provider: script
  script: scripts/publish
  